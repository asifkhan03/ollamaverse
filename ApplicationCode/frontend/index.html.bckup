<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AIOps Studio – Modern AIOps Platform</title>

<style>

/* ------------------------------------ */
/* ROOT VARIABLES */
/* ------------------------------------ */
:root{
  --green:#00ff99;
  --green-2:#00cc7a;
  --text-light:#eafff7;
  --dark-1:#003b2f;
  --dark-2:#001f17;
  --dark-3:#000f0c;
  --card-bg:rgba(255,255,255,0.05);
  --card-border:rgba(0,255,153,0.15);
}


/* ------------------------------------ */
/* RESET */
/* ------------------------------------ */
*{
  margin:0; padding:0;
  box-sizing:border-box;
  font-family:"Inter", Arial, sans-serif;
}

body{
  background: radial-gradient(circle at 30% 20%, var(--dark-1) 0%, var(--dark-2) 50%, var(--dark-3) 100%);
  color:var(--text-light);
  overflow-x:hidden;
}


/* ------------------------------------ */
/* PARTICLES */
/* ------------------------------------ */
.particle{
  position:absolute;
  width:6px; height:6px;
  background:var(--green);
  border-radius:50%;
  opacity:.7;
  animation: float 6s infinite ease-in-out;
  filter:blur(1px);
}

@keyframes float{
  0%{ transform:translate(0,0); opacity:.7;}
  50%{ transform:translate(20px,-30px); opacity:1;}
  100%{ transform:translate(0,0); opacity:.5;}
}


/* ------------------------------------ */
/* HEADER */
/* ------------------------------------ */
header{
  position:sticky;
  top:0;
  z-index:100;
  backdrop-filter: blur(18px) saturate(160%);
  background: rgba(0,25,18,0.45);
  border-bottom:1px solid rgba(0,255,153,0.15);
  padding:18px 50px;
}

nav{
  display:flex;
  align-items:center;
  justify-content:space-between;
}

.logo {
  display: flex;
  align-items: center;
  gap: 12px;
}

.logo-text {
  font-size: 1.35rem;
  font-weight: 800;
  background: linear-gradient(90deg, #00ff99, #00cc7a);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  letter-spacing: .5px;
}


.logo-circle {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(0,255,153,0.28);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 0 14px rgba(0,255,153,0.08);
}

.logo-circle svg {
  filter: drop-shadow(0 0 4px rgba(0,255,153,0.3));
}


.links{
  display:flex; gap:25px; align-items:center;
}

.links a{
  color:var(--text-light);
  font-size:.95rem;
  font-weight:600;
  text-decoration:none;
  position:relative;
  padding:8px 4px;
  transition:all .25s ease;
}

.links a:hover{
  color:var(--green);
  transform:translateY(-3px);
  text-shadow:0 0 8px rgba(0,255,153,.6);
}

.links a::after{
  content:'';
  position:absolute;
  bottom:-2px;
  left:0;
  width:0%;
  height:2px;
  background:var(--green);
  border-radius:2px;
  transition:width .25s ease;
}

.links a:hover::after{
  width:100%;
}


/* LOGIN BUTTON UPDATED */
.btn-login{
  background: rgba(0,0,0,0.45);
  padding:8px 22px;
  border-radius:12px;
  color:var(--green);
  font-weight:800;
  border:1px solid rgba(0,255,153,0.4);
  transition:all .25s ease;
}

.btn-login:hover{
  transform:translateY(-3px) scale(1.05);
  background:rgba(0,0,0,0.55);
  box-shadow:0 0 14px rgba(0,255,153,.6);
  border-color:var(--green);
}


/* ------------------------------------ */
/* HERO */
/* ------------------------------------ */
.hero{
  max-width:900px;
  text-align:center;
  margin:auto;
  padding:140px 20px 70px;
}

.tag{
  display:inline-block;
  color:var(--green);
  background:rgba(0,255,153,.12);
  padding:6px 18px;
  border-radius:20px;
  border:1px solid rgba(0,255,153,.25);
  font-size:.85rem;
  margin-bottom:18px;
}

.hero h1{
  font-size:3.2rem;
  font-weight:800;
  line-height:1.2;
  margin-bottom:15px;
}

.hero p{
  font-size:1.2rem;
  opacity:.9;
  margin-bottom:30px;
}

.btn-main, .btn-outline{
  display:inline-block;
  padding:12px 28px;
  border-radius:14px;
  font-weight:700;
  margin:10px;
  cursor:pointer;
  transition:all .25s ease;
}

.btn-main{
  background:linear-gradient(90deg,var(--green),var(--green-2));
  color:#06251a;
}

.btn-main:hover{
  transform:translateY(-5px) scale(1.05);
  box-shadow:0 0 20px rgba(0,255,153,.55);
}

.btn-outline{
  border:2px solid var(--green);
  color:var(--green);
}

.btn-outline:hover{
  background:rgba(0,255,153,.15);
  transform:translateY(-5px);
}


/* ------------------------------------ */
/* VIDEO SECTION (UPDATED) */
/* ------------------------------------ */
.video-section{
  padding:70px 20px;
  display:flex;
  justify-content:center;
}

.video-container{
  max-width:1000px;
  width:100%;
  padding:25px;
  border-radius:25px;
  background:rgba(255,255,255,0.04);
  border:1px solid rgba(0,255,153,0.18);
  box-shadow:0 0 40px rgba(0,255,153,0.12);
  backdrop-filter:blur(10px);
  transition:all .35s ease;
}

.video-container:hover{
  transform:scale(1.01);
  box-shadow:0 0 55px rgba(0,255,153,0.25);
}

#demoVideo{
  width:100%;
  border-radius:20px;
  margin-bottom:28px;
}


/* VIDEO CONTROLS */
.video-controls{
  display:flex;
  justify-content:center;
  gap:25px;
  flex-wrap:wrap;
  margin-top:10px;
}

.control-btn{
  padding:12px 28px;
  font-size:1rem;
  font-weight:700;
  border-radius:14px;
  border:1px solid rgba(0,255,153,0.35);
  background:rgba(0,255,153,0.08);
  color:var(--green);
  cursor:pointer;
  transition:all .25s ease;
  backdrop-filter:blur(10px);
}

.control-btn:hover{
  transform:translateY(-5px) scale(1.05);
  background:rgba(0,255,153,0.18);
  box-shadow:0 0 18px rgba(0,255,153,.45);
  border-color:var(--green);
}


/* ------------------------------------ */
/* FEATURES */
/* ------------------------------------ */
.features{
  max-width:1100px;
  margin:80px auto;
  padding:0 20px;
  text-align:center;
}

.features h2{
  font-size:2.4rem;
  margin-bottom:40px;
}

.feature-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:25px;
}

.feature-card{
  background:var(--card-bg);
  border:1px solid var(--card-border);
  padding:26px;
  border-radius:18px;
  backdrop-filter:blur(8px);
  transition:all .28s ease;
}

.feature-card:hover{
  transform:translateY(-12px) scale(1.04);
  border-color:var(--green);
  box-shadow:0 0 25px rgba(0,255,153,.25);
}

.feature-card h3{
  color:var(--green);
  font-size:1.3rem;
  margin-bottom:12px;
}


/* ------------------------------------ */
/* ABOUT */
/* ------------------------------------ */
.about{
  max-width:800px;
  margin:80px auto;
  padding:0 20px;
  text-align:center;
}

.about h2{
  font-size:2.4rem;
  margin-bottom:25px;
}


/* ------------------------------------ */
/* FAQ */
/* ------------------------------------ */
.faq{
  max-width:900px;
  margin:90px auto;
  padding:0 20px;
}

.faq h2{
  text-align:center;
  font-size:2.3rem;
  margin-bottom:35px;
}

details{
  background:var(--card-bg);
  border:1px solid var(--card-border);
  padding:15px 20px;
  border-radius:15px;
  margin-bottom:15px;
  cursor:pointer;
  transition:all .25s ease;
}

details:hover{
  border-color:var(--green);
  box-shadow:0 0 15px rgba(0,255,153,.25);
}

summary{
  font-size:1.1rem;
  font-weight:700;
}

details p{
  margin-top:10px;
  line-height:1.6;
  opacity:.85;
}


/* ------------------------------------ */
/* FOOTER */
/* ------------------------------------ */
footer{
  text-align:center;
  padding:25px;
  opacity:.7;
  margin-top:70px;
}

</style>
</head>



<body>

<!-- PARTICLES -->
<div class="particle" style="top:20%; left:15%; animation-delay:0s;"></div>
<div class="particle" style="top:65%; left:10%; animation-delay:1s;"></div>
<div class="particle" style="top:35%; left:80%; animation-delay:.7s;"></div>
<div class="particle" style="top:80%; left:60%; animation-delay:1.4s;"></div>


<!-- HEADER -->
<header>
  <nav>
<div class="logo">
  <div class="logo-circle">
    <!-- Custom AIOps Studio SVG Logo -->
    <svg viewBox="0 0 64 64" width="24" height="24" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <linearGradient id="g" x1="0" x2="1" y1="0" y2="1">
          <stop offset="0%" stop-color="#00ff99"/>
          <stop offset="100%" stop-color="#00cc7a"/>
        </linearGradient>
      </defs>

      <!-- Outer chip shape -->
      <rect x="8" y="8" width="48" height="48" rx="10" ry="10"
            fill="rgba(255,255,255,0.02)"
            stroke="url(#g)" stroke-width="1.8"/>

      <!-- Inner highlight box -->
      <rect x="16" y="16" width="32" height="32" rx="6"
            fill="transparent" stroke="rgba(255,255,255,0.08)" stroke-width="1"/>

      <!-- Circuit lines -->
      <path d="M22 20 L28 20 L34 26 L38 26"
            stroke="url(#g)" stroke-width="1.6" stroke-linecap="round" fill="none"/>
      <path d="M22 28 L26 28 L30 32 L38 32"
            stroke="rgba(0,255,153,0.65)" stroke-width="1.2" stroke-linecap="round" fill="none"/>

      <!-- Nodes -->
      <circle cx="22" cy="20" r="1.6" fill="url(#g)" />
      <circle cx="38" cy="26" r="1.6" fill="url(#g)" />
      <circle cx="30" cy="32" r="1.6" fill="url(#g)" />
    </svg>
  </div>

  <span class="logo-text">AIOps Studio</span>
</div>


    <div class="links">
      <a href="#">Home</a>
      <a href="#video">Demo</a>
      <a href="#features">Features</a>
      <a href="#about">About</a>
      <a href="#faq">FAQ</a>
      <a href="login.html" class="btn-login">Login</a>
    </div>
  </nav>
</header>



<!-- HERO -->
<section class="hero">
  <div class="tag">Next-generation AIOps</div>

  <h1>AI-driven workflows for modern ops teams</h1>

  <p>Ask questions, triage incidents, automate fixes, and get insights — all via natural language.</p>

  <a href="login.html" class="btn-main">Get Started</a>
  <a href="#video" class="btn-outline">Watch Demo</a>
</section>



<!-- VIDEO SECTION (UPDATED) -->
<section id="video" class="video-section">
  <div class="video-container">

    <video id="demoVideo" autoplay muted loop>
      <source src="" type="video/mp4">
    </video>

    <div class="video-controls">
      <button class="control-btn" onclick="playVideo()">▶ Play Demo</button>
      <button class="control-btn" onclick="pauseVideo()">⏸ Pause</button>
      <button class="control-btn" onclick="fullscreenVideo()">🟢 Fullscreen</button>
    </div>

  </div>
</section>



<!-- FEATURES -->
<section id="features" class="features">
  <h2>Powerful Features</h2>

  <div class="feature-grid">

    <div class="feature-card">
      <h3>AI-driven Diagnostics</h3>
      <p>Ask questions in natural language and get immediate insights about system issues, logs, and performance.</p>
    </div>

    <div class="feature-card">
      <h3>Token-based Access</h3>
      <p>Secure API token generation for user authentication and controlled access.</p>
    </div>

    <div class="feature-card">
      <h3>Model Flexibility</h3>
      <p>Switch between models like SmolLM2 and TinyLlama based on your workload.</p>
    </div>

    <div class="feature-card">
      <h3>Automation Support</h3>
      <p>Enable scripts, commands, or automations using simple chat instructions.</p>
    </div>

    <div class="feature-card">
      <h3>Observability Ready</h3>
      <p>Easily integrate logs, metrics, and events for AI-based troubleshooting.</p>
    </div>

    <div class="feature-card">
      <h3>Secure by Design</h3>
      <p>All operations run inside your environment — your data never leaves your control.</p>
    </div>

  </div>
</section>

<!-- ABOUT -->
<section id="about" class="about">
  <h2>About AIOps Studio</h2>
  <p>
    AIOps Studio is a modern AIOps platform powered by secure local LLMs.  
    Built for reliability, privacy, and fast operational insight generation.
  </p>
</section>

<!-- FAQ -->
<section id="faq" class="faq">
  <h2>Frequently Asked Questions</h2>

  <details>
    <summary>What is AIOps Studio?</summary>
    <p>AIOps Studio helps engineers troubleshoot systems, automate fixes, and analyze issues through natural language.</p>
  </details>

  <details>
    <summary>How do I sign up?</summary>
    <p>Click “Get Started” to create an account and begin using the platform instantly.</p>
  </details>

  <details>
    <summary>Which AI models are supported?</summary>
    <p>SmolLM2, TinyLlama, and custom LLMs depending on your setup.</p>
  </details>

</section>

<!-- FOOTER -->
<footer>
  © 2025 AIOps Studio — All rights reserved.
</footer>

<script>
const video = document.getElementById("demoVideo");

function playVideo(){
  video.play();
}

function pauseVideo(){
  video.pause();
}

function fullscreenVideo(){
  if(video.requestFullscreen){
    video.requestFullscreen();
  } else if(video.webkitRequestFullscreen){
    video.webkitRequestFullscreen();
  } else if(video.msRequestFullscreen){
    video.msRequestFullscreen();
  }
}
</script>

</body>
</html>

